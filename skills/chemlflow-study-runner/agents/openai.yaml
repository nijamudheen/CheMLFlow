interface:
  display_name: "CheMLFlow Study Runner"
  short_description: "Plan and run approval-gated CheMLFlow studies"
  default_prompt: "Use $chemlflow-study-runner to profile this dataset, ask the required study-design questions, prepare a dashboard preview, and run only after my approval."
